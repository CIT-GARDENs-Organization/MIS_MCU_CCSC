#INT_TIMER0
static void TIMER0_isr(void)
{
   // every 0.01 sec interrupts
   set_timer0(100);

   if (++subsec >= 100)
   {
      subsec = 0;
      sec++;

      if (sec >= SEC_IN_A_DAY)
      {
         sec -= SEC_IN_A_DAY;
         day++;
      }
   }
}
